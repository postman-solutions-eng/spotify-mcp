$kind: mcp-request
description: |-
  **Purpose:** page the user's Liked Songs library.

  **Result** (Executed 2026-08-25 against spotify-mcp-jamiew 0.4.1 (stdio)): `total: 2` — "Und dann kommt ein Mensch" — Major Sunburn (added
  2026-08-25T15:16:05Z, by the "Like track" request in this collection) and
  "Labyrinth of Mirrors" — Aimoon, Victor Tayne (added 2026-08-24T17:21:45Z).

  Unlike playlists, this endpoint reports `total` honestly.
transport: stdio
command: uvx spotify-mcp-jamiew
env:
  SPOTIFY_CLIENT_ID: "{{SPOTIFY_CLIENT_ID}}"
  SPOTIFY_REDIRECT_URI: "{{SPOTIFY_REDIRECT_URI}}"
  SPOTIFY_CLIENT_SECRET: "{{SPOTIFY_CLIENT_SECRET}}"
message: |-
  {
    "method": "tools/call",
    "params": {
      "name": "get_saved_tracks",
      "arguments": {
        "limit": 5,
        "offset": 0
      }
    }
  }
settings:
  version: auto
  requestTimeout: 0
order: 31000
